org.gradle.jvmargs=-Xmx2G -Xms512m -XX:MaxMetaspaceSize=512m -XX:ReservedCodeCacheSize=256m -XX:+UseG1GC -XX:MaxGCPauseMillis=200 -Dfile.encoding=UTF-8
android.useAndroidX=true
android.enableJetifier=true
# Limit Gradle worker parallelism to reduce native memory pressure on smaller dev machines
org.gradle.workers.max=1
# Disable Gradle daemon to free up memory after build
org.gradle.daemon=false
